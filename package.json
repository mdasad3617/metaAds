{
  "name": "meta-ads-ai-generator",
  "version": "1.0.0",
  "description": "AI-powered Meta (Facebook) ads generator with TypeORM and modern architecture",
  "private": true,

  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "build": "npm run server:build && npm run client:build",
    "server:build": "cd server && npm run build",
    "client:build": "cd client && npm run build",
    "start": "cd server && npm run start:prod",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd ../shared && npm install",
    "clean": "rm -rf node_modules server/node_modules client/node_modules shared/node_modules",
    "lint": "cd server && npm run lint && cd ../client && npm run lint",
    "test": "cd server && npm run test"
  },
  "keywords": ["meta", "facebook", "ads", "ai", "generator", "marketing", "typeorm", "nestjs", "nextjs"],
  "author": "AdVision AI",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}